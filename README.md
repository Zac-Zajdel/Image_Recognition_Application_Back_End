# Image_Recognition_Application_Back_End

Creates a server that connects to a database which can do the following:
  - Register new users
  - Verify a users sign-in credentials
  - Retrieve data about users
